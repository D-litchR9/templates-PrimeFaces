<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ejemplo</groupId>
    <artifactId>miapp-web</artifactId>
    <version>1.0.0</version>
    <packaging>war</packaging>

    <name>MiApp Web</name>
    <description>Aplicación Java web con PrimeFaces, JSF, CDI y Tomcat</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Versiones Jakarta EE 10 -->
        <jakarta-servlet-api.version>6.0.0</jakarta-servlet-api.version>
        <jakarta-faces.version>4.0.5</jakarta-faces.version>   <!-- Mojarra -->
        <primefaces.version>13.0.5</primefaces.version>
        <weld.version>5.1.2.Final</weld.version>               <!-- CDI -->
        <jakarta-el.version>4.0.2</jakarta-el.version>         <!-- Expression Language -->
        <jakarta-annotation-api.version>2.1.1</jakarta-annotation-api.version>
    </properties>

    <dependencies>
        <!-- Servlet API (proporcionado por Tomcat) -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>${jakarta-servlet-api.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Jakarta Faces (Mojarra) - implementación JSF -->
        <dependency>
            <groupId>org.glassfish</groupId>
            <artifactId>jakarta.faces</artifactId>
            <version>${jakarta-faces.version}</version>
        </dependency>

        <!-- PrimeFaces - componentes JSF -->
        <dependency>
            <groupId>org.primefaces</groupId>
            <artifactId>primefaces</artifactId>
            <version>${primefaces.version}</version>
            <classifier>jakarta</classifier> <!-- para namespace Jakarta -->
        </dependency>

        <!-- Weld - implementación CDI para Tomcat -->
        <dependency>
            <groupId>org.jboss.weld.servlet</groupId>
            <artifactId>weld-servlet-core</artifactId>
            <version>${weld.version}</version>
        </dependency>

        <!-- Expression Language (necesario para JSF en Tomcat) -->
        <dependency>
            <groupId>org.glassfish</groupId>
            <artifactId>jakarta.el</artifactId>
            <version>${jakarta-el.version}</version>
        </dependency>

        <!-- Jakarta Annotations (requerido por CDI y JSF) -->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>${jakarta-annotation-api.version}</version>
            <scope>provided</scope> <!-- Tomcat lo incluye -->
        </dependency>
    </dependencies>

    <build>
        <finalName>miapp</finalName>
        <plugins>
            <!-- Plugin compilador para Java 21 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>

            <!-- Plugin WAR para evitar fallo si no hay web.xml -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>